<%= link_to "Edit Attraction", edit_attraction_path if @user.admin %>
<%= @user.name %>
<%= @attraction.name %>

<%= form_tag('/ride') do %>
  <%= hidden_field_tag :attraction_id, @attraction.id %>
  <%= hidden_field_tag :user_id, @user.id %>
  <%= submit_tag "Go on this ride" %>
<% end %>
